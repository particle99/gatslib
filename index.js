const { Client } = require("./src/client");
const { Captcha } = require("./src/captcha"); //not used (as of now)

//check ./src/config.js to change
const { config } = require("./src/config");

//create client..

//listen for events..

